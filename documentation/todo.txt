SpriteTile
	height
	continuous (bottomless)
	pixel factors: width, length, height
	rect:
		top - width x length
		middle - width x height factor x height
		bottom - width x length
	draw bottom to top, skip bottom if cont

ZObject(TransformableDrawable) -> use that self.position
add ZList to map -> need to use proper states during draw,
but list orders the draw